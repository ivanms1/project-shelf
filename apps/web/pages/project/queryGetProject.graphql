query GetProject($id: String!) {
  project: getProject(id: $id) {
    id
    title
    preview
    repoLink
    siteLink
    description
    isApproved
    likesCount
    createdAt
    tags
    author {
      id
      name
      avatar
    }
    isLiked
  }
}
