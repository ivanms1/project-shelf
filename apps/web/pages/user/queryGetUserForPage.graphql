query GetUserForPage($id: ID!) {
  user: getUser(id: $id) {
    id
    name
    email
    github
    avatar
    projects {
      id
      title
      preview
      likesCount
      isLiked
      tags
      description
      siteLink
      repoLink
    }
    isFollowing
    followerCount
  }
}
