query GetAllUsers {
  users: getUsers {
    nextCursor
    prevCursor
    totalCount
    results {
      id
      name
      lastName
      email
      role
      projects {
        id
        title
        preview
        description
        createdAt
        siteLink
        repoLink
        isApproved
      }
    }
  }
}
