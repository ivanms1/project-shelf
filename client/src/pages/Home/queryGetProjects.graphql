#import "../../graphql/projectFragment.graphql"

query GetAllProjects($cursor: String = null, $modifiers: GetProjectsFilter) {
  projects: getProjects(cursor: $cursor, modifiers: $modifiers) {
    ...ProjectsResponseFragment
  }
}
